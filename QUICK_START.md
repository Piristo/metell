# 🚀 Быстрый старт Metallica Archive Bot

## Шаг 1: Установите Python

1. Скачайте Python 3.11+ с https://www.python.org/downloads/
2. При установке **ОБЯЗАТЕЛЬНО** отметьте:
   - ☑️ Add Python to PATH
3. Проверьте установку:
   ```cmd
   python --version
   ```

## Шаг 2: Получите токены

### Telegram Bot Token:
1. Откройте Telegram и найдите @BotFather
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. **Скопируйте токен** (пример: `123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11`)

### YouTube API Key:
1. Перейдите в Google Cloud Console: https://console.cloud.google.com
2. Создайте новый проект
3. В меню выберите **APIs & Services → Library**
4. Найдите и включите **YouTube Data API v3**
5. В меню выберите **APIs & Services → Credentials**
6. Нажмите **CREATE CREDENTIALS → API key**
7. **Скопируйте ключ**

## Шаг 3: Настройте бота

Откройте файл `.env` в редакторе:

```env
TELEGRAM_BOT_TOKEN=ВАШ_ТЕЛЕГРАМ_ТОКЕН
YOUTUBE_API_KEY=ВАШ_YOUTUBE_API_КЛЮЧ
REDIS_URL=redis://localhost:6379
DATABASE_URL=sqlite:///./data/metallica.db
MAX_RESULTS_PER_PAGE=10
SYNC_INTERVAL_HOURS=24
```

**Замените на ваши реальные токены!**

## Шаг 4: Установите зависимости

### Вариант A: Скрипт (рекомендуется)
```cmd
install.bat
```

### Вариант B: Вручную
```cmd
pip install -r requirements.txt
```

## Шаг 5: Запустите бота

```cmd
run.bat
```

Или напрямую:
```cmd
python bot\main.py
```

## 🎉 Готово!

Бот должен ответить:
```
🎸 Metallica Archive Bot
Добро пожаловать в архив лучших концертов и интервью Metallica!
```

## 📱 Использование в Telegram

Откройте вашего бота и используйте команды:

```
🎸 /concerts    - Полные концерты
🎤 /interviews  - Полные интервью
📦 /archive     - Хронологический архив
🎫 /tour M72    - Конкретный тур
📅 /year 1991   - Записи за год
🔄 /refresh     - Обновить базу
📊 /stats       - Статистика
/help           - Помощь
```

## 🐛 Если возникли проблемы

### "Python не найден"
- Переустановите Python с галочкой "Add Python to PATH"
- Перезапустите командную строку

### "Module not found"
- Выполните: `pip install -r requirements.txt`

### "Telegram bot token invalid"
- Проверьте токен в файле `.env`
- Убедитесь, что нет лишних пробелов

### "YouTube API quota exceeded"
- Подождите 24 часа или создайте новый API ключ
- Бесплатная квота: 100 запросов/день

## 📂 Структура проекта

```
metallica-archive-bot/
├── .env              ← Ваши токены здесь!
├── requirements.txt  ← Зависимости
├── bot/
│   ├── main.py       ← Запуск бота
│   └── handlers/    ← Команды
├── database/         ← SQLite + Redis
├── services/         ← YouTube API
└── data/            ← Туры, ключевые слова
```

## 🔧 Дополнительные команды

```cmd
# Инициализация БД
python init_db.py init

# Статистика
python init_db.py stats

# Только туры
python init_db.py tours
```

## 📞 Поддержка

Если что-то не работает:
1. Проверьте логи в консоли
2. Убедитесь, что токены корректны
3. Проверьте подключение к интернету
